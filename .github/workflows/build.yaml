name: Cross-platform Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller -r requirements.txt
      - name: Build Windows EXE
        run: |
          pyinstaller --onefile --noconsole --name LP_Generator-Windows src/main.py
      - name: Upload Windows artifact
        uses: actions/upload-artifact@v3
        with:
          name: windows-build
          path: dist/LP_Generator-Windows.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          sudo apt-get install -y python3-tk
          python -m pip install --upgrade pip
          pip install pyinstaller -r requirements.txt
      - name: Build Linux binary
        run: |
          pyinstaller --onefile --name LP_Generator-Linux src/main.py
      - name: Upload Linux artifact
        uses: actions/upload-artifact@v3
        with:
          name: linux-build
          path: dist/LP_Generator-Linux

  create-release:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v3
        with:
          name: windows-build
          path: dist

      - name: Download Linux artifact
        uses: actions/download-artifact@v3
        with:
          name: linux-build
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref }}
          files: |
            dist/LP_Generator-Windows.exe
            dist/LP_Generator-Linux
          body: |
            Кроссплатформенный релиз LP Generator
            Поддерживаемые ОС:
            - Windows 10/11
            - Linux (Ubuntu-based)